{
  "name": "zogra-renderer",
  "version": "1.0.0",
  "description": "A simple renderer based on WebGL.",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "build:watch": "tsc -w"
  },
  "keywords": [
    "renderer",
    "webgl"
  ],
  "author": "SardineFish",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.7.4"
  },
  "dependencies": {
    "@types/gl-matrix": "^2.4.5",
    "gl-matrix": "^3.1.0",
    "linq": "^3.2.1",
    "reflect-metadata": "^0.1.13"
  }
}
